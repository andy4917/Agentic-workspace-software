# Windows Dev Terms

Use Windows-facing terminology in user-visible reports:

- `directory` -> `folder`
- `shell` -> `PowerShell`
- `command line` -> `PowerShell`

Keep config keys, file names, code, identifiers, and PowerShell literals unchanged.

