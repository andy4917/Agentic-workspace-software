diff --git a/lib/config.js b/lib/config.js
index 768c116..ebdc77d 100755
--- a/lib/config.js
+++ b/lib/config.js
@@ -292,6 +292,7 @@ export const LLM_TOKEN_BUDGET_WINDOW_SEC = Number(process.env.LLM_TOKEN_BUDGET_W
 /** NLI 서비스 설정 (미설정 시 in-process ONNX 모델 로드) */
 export const NLI_SERVICE_URL    = process.env.NLI_SERVICE_URL || "";
 export const NLI_TIMEOUT_MS     = Number(process.env.NLI_TIMEOUT_MS || 5000);
+export const NLI_ENABLED        = process.env.NLI_ENABLED !== "false";
 
 /**
  * Case event 검증 결과(verification_passed/failed)를 증거 파편 importance에 역전파할지 여부.
@@ -309,6 +310,7 @@ export const CASE_BACKPROP_ENABLED = process.env.MEMENTO_CASE_BACKPROP_ENABLED =
 export const RERANKER_URL        = process.env.RERANKER_URL || "";
 export const RERANKER_TIMEOUT_MS = Number(process.env.RERANKER_TIMEOUT_MS || 5000);
 export const RERANKER_MODEL      = (process.env.RERANKER_MODEL || "minilm").toLowerCase();
+export const RERANKER_ENABLED    = process.env.RERANKER_ENABLED !== "false";
 
 /** Fragment 쿼터 기본값 */
 export const FRAGMENT_DEFAULT_LIMIT = process.env.FRAGMENT_DEFAULT_LIMIT
diff --git a/lib/memory/Reranker.js b/lib/memory/Reranker.js
index fff5097..783b5f2 100644
--- a/lib/memory/Reranker.js
+++ b/lib/memory/Reranker.js
@@ -17,11 +17,11 @@
  *   - cross-encoder: [query, document] 쌍으로 relevance score 출력
  */
 
-import { RERANKER_URL, RERANKER_TIMEOUT_MS, RERANKER_MODEL } from "../config.js";
+import { RERANKER_ENABLED, RERANKER_URL, RERANKER_TIMEOUT_MS, RERANKER_MODEL } from "../config.js";
 import { logInfo, logWarn } from "../logger.js";
 
 /** ─── 공유 상태 ─── */
-let _mode                      = RERANKER_URL ? "external" : "inprocess";
+let _mode                      = !RERANKER_ENABLED ? "disabled" : (RERANKER_URL ? "external" : "inprocess");
 let _failed                    = false;
 let _consecutiveExternalFails  = 0;
 const EXTERNAL_FAIL_THRESHOLD  = 3;
@@ -173,6 +173,7 @@ async function rerankInProcess(query, documents) {
  * 임계치 초과 시 rerank() 내부에서 inprocess로 자동 전환된다.
  */
 export function isRerankerAvailable() {
+  if (_mode === "disabled") return false;
   if (_mode === "external") return _consecutiveExternalFails < EXTERNAL_FAIL_THRESHOLD;
   return !_failed;
 }
@@ -204,6 +205,7 @@ function computeRecencyBoost(createdAt) {
  */
 export async function rerank(query, candidates, topK = 15) {
   if (!candidates || candidates.length === 0) return [];
+  if (_mode === "disabled") return candidates;
 
   /** Hindsight 패턴: content에 날짜 프리픽스 추가 */
   const documents = candidates.map(c => {
@@ -248,6 +250,11 @@ export async function rerank(query, candidates, topK = 15) {
  * External 모드: 헬스체크, In-Process 모드: 모델 로드
  */
 export async function preloadReranker() {
+  if (_mode === "disabled") {
+    logInfo("[Reranker] Disabled by RERANKER_ENABLED=false");
+    return;
+  }
+
   if (_mode === "external") {
     try {
       const res = await fetch(`${RERANKER_URL}/health`, {
diff --git a/lib/memory/signals/NLIClassifier.js b/lib/memory/signals/NLIClassifier.js
index 689c9da..5bc8597 100644
--- a/lib/memory/signals/NLIClassifier.js
+++ b/lib/memory/signals/NLIClassifier.js
@@ -22,11 +22,11 @@
  *   3. Gemini CLI 에스컬레이션 → 수치/도메인 모순 처리
  */
 
-import { NLI_SERVICE_URL, NLI_TIMEOUT_MS } from "../../config.js";
+import { NLI_ENABLED, NLI_SERVICE_URL, NLI_TIMEOUT_MS } from "../../config.js";
 import { logInfo, logWarn } from "../../logger.js";
 
 /** ─── 공유 상태 ─── */
-let _mode    = NLI_SERVICE_URL ? "external" : "inprocess";
+let _mode    = !NLI_ENABLED ? "disabled" : (NLI_SERVICE_URL ? "external" : "inprocess");
 let _failed  = false;
 
 /** ─── In-Process 전용 상태 ─── */
@@ -179,6 +179,7 @@ async function classifyInProcess(premise, hypothesis) {
  * NLI 모델 사용 가능 여부 (동기)
  */
 export function isNLIAvailable() {
+  if (_mode === "disabled") return false;
   if (_mode === "external") return true;
   return !_failed;
 }
@@ -191,6 +192,7 @@ export function isNLIAvailable() {
  * @returns {Promise<{label: string, scores: {entailment: number, neutral: number, contradiction: number}} | null>}
  */
 export async function classifyNLI(premise, hypothesis) {
+  if (_mode === "disabled") return null;
   if (_mode === "external") {
     return classifyExternal(premise, hypothesis);
   }
@@ -243,6 +245,11 @@ export async function detectContradiction(contentA, contentB) {
  * External 모드: 헬스체크, In-Process 모드: 모델 로드
  */
 export async function preloadNLI() {
+  if (_mode === "disabled") {
+    logInfo("[NLIClassifier] Disabled by NLI_ENABLED=false");
+    return;
+  }
+
   if (_mode === "external") {
     try {
       const res = await fetch(`${NLI_SERVICE_URL}/health`, {
