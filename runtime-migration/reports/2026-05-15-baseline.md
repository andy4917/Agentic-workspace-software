# Runtime Migration Baseline

Date: 2026-05-15

## Scope

- Root: `%USERPROFILE%\.codex`
- Work order: start the Rust/Python split with inventory and contracts only.
- Memento MCP: excluded from rewrite decisions and kept behind the existing
  verifier and memory ceiling.

## Baseline Evidence

- `python -m py_compile` for harness modules: pass.
- `codex_agent_harness.py doctor --json`: pass.
- `check-toolchain-sources.ps1`: pass.
- `node_js_surface_scanner.py`: generated 27 compact surface tickets; 2 are
  Memento markers with `migration_decision=keep`.
- `python -m unittest discover -s runtime-migration/tests -v`: pass.
- `codex_agent_harness.py eval --eval-id hook-policy-smoke`: pass.
- `check-worktree-sensitive-diff.ps1`: pass, findings 0.
- `memento-mcp-runtime.ps1 verify`: pass; observed
  `postgres_ready=True`, `memento_health=True`,
  `MEMENTO_INPROCESS_ONNX_ENABLED=false`,
  `MEMENTO_MANAGED_EMBEDDING_PROVIDER=none`, and working set below `512 MB`.

## First Slice

- Python scanner: `runtime-migration/python/runtime_migration/node_js_surface_scanner.py`
- Inventory output: `runtime-migration/inventory/node-js-surfaces.yaml`
- Contract templates: `runtime-migration/contracts/`
- Memento exclusion regression: `runtime-migration/tests/test_node_js_surface_scanner.py`

## Rollback

This slice has no active runtime switch. Rollback is removal of
`runtime-migration/` plus the Vowline fixture text update if the user rejects the
direction.
