# Codex plugin guard root

Plugin support can remain enabled while known bundled marketplace/cache regeneration paths are blocked by exact-name sentinel files.
Do not use `cache`, `plugins`, `local-marketplaces\openai-bundled`, or `local-marketplaces\openai-primary-runtime` as active sources.
