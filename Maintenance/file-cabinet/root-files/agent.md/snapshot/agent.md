# agent.md

`AGENTS.md` is the canonical agent contract for this GlobalSSOT root.

This lower-priority file exists only as a pointer. Do not maintain a separate
abbreviated contract here. Agents working under
`%USERPROFILE%\.codex` must read and follow `AGENTS.md`, plus any
higher-priority system, developer, tool, and user instructions.

