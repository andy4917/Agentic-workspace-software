# Settings

Settings for `Dev_Codex_App_GlobalSSOT`.

Folders:

- `Codex_App_DECLARATIVE`: machine-readable policy and reliability fixtures.
- `Codex_App_RUNTIME`: runtime state files and schema declarations.
- `Dev_Codex_HOOKS`: hook configs and the shared Codex hook runner.

