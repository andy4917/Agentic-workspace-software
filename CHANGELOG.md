# Changelog

## 2026-05-08

- Adjusted the live Codex hook display policy in `C:\Users\anise\.codex\hooks.json` so only SessionStart and Stop keep visible `status_message` text; UserPromptSubmit, PreToolUse, and PostToolUse continue running silently for state, guard, and ledger evidence.
- Rewrote `AGENTS.md` for the current Codex App GlobalSSOT environment, including `[features].hooks`, protected global config boundaries, append-only runtime ledgers, Spark inspector dedupe semantics, connected-surface review, and the current PowerShell verification commands; reduced `AGENT.md` to a canonical alias to avoid stale duplicate guidance.
- Made Spark inspector enqueue idempotent with a `parent_turn_id + route_id + normalized target set` `dedupe_key`, append-only `duplicate_of` markers for suppressed duplicate queue attempts, and Stop/context read paths that ignore duplicate/superseded job records while preserving ledger history.
- Updated the subagent inspection routing regression to reproduce duplicate enqueue attempts and prove only one active queued job is counted for the same dedupe key.
- Removed direct Korean string literals from the PowerShell hook runner classification paths so `powershell.exe` can parse the UTF-8 no-BOM script reliably, and enabled the new `[features].hooks` flag in the user Codex config to address the deprecated `[features].codex_hooks` warning.
- Added Harness V2 skill-route integration: fixed `addyosmani/agent-skills` as the upstream source, installed the P0 skill subset under `C:\Users\anise\.agents\skills`, and recorded source HEAD plus copied `SKILL.md` hashes in `Maintenance/agent-skills-integration/agent_skills_inventory.json`.
- Extended live receipts from `task_classification -> need_resolution -> required route -> Stop` to include `skill_resolution_receipt.v1` and `skill_usage_event.v1`, with required skill evidence checked only at Stop unless an action is an absolute risk.
- Added skill-route policy wiring to the hook runner, runtime schema, manifest, `required-tool-routes.json`, and workflow/tool-skill orchestration config; installed/configured skills remain non-evidence unless a real `skill_usage_event` or explicit unavailable/not_applicable record exists.
- Propagated required skills into worker job envelopes and inspector compliance targets, kept subagent reports candidate-only, and narrowed frontend/backend route matching to path or explicit adoption evidence instead of catalog role words.
- Cleaned contaminated skill usage ledger entries generated by the earlier broad text matcher and kept only events whose `skill_name` matches an installed `SKILL.md` directory.

## 2026-05-07

- Split subagent orchestration into `worker_routes` and `inspector_routes`: mutating Class 2/3/4 work now requires worker spawn evidence instead of satisfying "use subagents" with Spark inspectors only; inspectors remain read-only candidate evidence, workers produce candidate artifacts, and Stop/gate-issued receipts remain completion authority.
- Added PM lifecycle enforcement for subagents: automatic close lifecycle events after report review/route resolution, `agents.max_threads = 8`, `agents.max_depth = 1`, worker/inspector model policy, and Harness V2 coverage for inspector-only delegation failure, worker+inspector success, candidate-only report authority, and missing worker report/spawn reason codes.
- Wired live PM preflight to distinguish `required_worker_routes` from `required_inspector_routes`, require worker spawn evidence for mutating tasks, and reject collapsed worker-to-inspector delegation with explicit PM failure reason codes.
- Added repo-specific `repo_v2_adoption_receipt.v1` flow: runtime schema/state registration, Stop gate validation, gate-issued receipt linkage, repo path and Git dirty-state capture, active agent chain, lint/typecheck/test/build evidence, required routes, inspector reports, contamination scan, gate decision, and handoff readiness.
- Added `Maintenance/Test-RepoV2AdoptionReceiptV2.ps1` as the clean verifier/generator for `Settings/Codex_App_RUNTIME/repo_v2_adoption_receipt.json`, and removed the contaminated first verifier candidate instead of keeping it as an alternate route.
- Expanded Harness V2 acceptance coverage from 72 to 76 cases with repo_v2 missing receipt, gate conflict, handoff-not-ready, and contamination-scan-failed completion blockers.
- Hardened Spark inspector job payloads so `target_paths` are sanitized to existing absolute Windows paths at extraction, job append, ledger append, and Stop read time; added regression coverage for `test/build,`,` prompt contamination across `required_tool_route_inspection` and `ssot_contract_inspection`, plus the PostToolUse bare-report regex failure that produced `hook exited with code 1`.
- Added Need Resolution P0 enforcement: `task_classification_receipt.v1`, `need_resolution_receipt.v1`, positive-allowlist basic classification, upward ambiguity handling, Stop-only UNKNOWN/unsatisfied route denial, PM accountability checks, and required route satisfaction evidence.
- Synced runtime capability receipt generation with the latest global config values: disabled MCP servers are recorded as disabled, runtime worker subagents carry `gpt-5.5`/medium/`max_depth=1`, and runtime schema declares those trust-state fields.
- Initialized `Dev_Codex_App_GlobalSSOT` as a Git repository on `main`, added local Git identity/config, and added `.gitignore`/`.gitattributes` safeguards for runtime logs, caches, dependencies, upstream mirrors, and line endings.
- Documented recursive subagent spawning as capped at `max_depth=1`; concurrency belongs to thread/job limits, not recursive depth.
- Documented the worker/inspector split: worker subagents use `gpt-5.5` with medium reasoning for actual code work, while Spark inspectors stay read-only `gpt-5.3-codex-spark`/high candidate-evidence helpers.
- Added Harness V2 final acceptance candidate evidence outputs: `final_acceptance_result.json` and `final_acceptance_report.md`, then registered them in the root manifest and inventory.
- Added Harness V2 PM subagent lifecycle handling: candidate-only inspector report ledger, `subagent_lifecycle_event.v1` ledger, `pm_decision_event.v1` compatibility events, Stop denial for quarantined/terminated worker reports, and replacement-limit `STOP_AND_REPORT`.
- Expanded isolated Harness V2 acceptance coverage from 52 to 60 cases, including subagent PASS non-authority, fabricated worker validation reports, repeated missing evidence, tainted report adoption, quarantined/terminated report denial, clean replacement report satisfaction, and replacement-loop stop behavior.
- Updated runtime schema, completion gate actions, manifest, and inventories for the new report/lifecycle ledgers.
- Added Main Agent Accountability to isolated Harness V2: PM authority now carries PM responsibility, routed executable/procedural completion claims require PM aggregation evidence, and PM failures deny completion credit without making PreToolUse stricter.
- Added `pm_decision.v1` runtime schema, `Settings/Codex_App_RUNTIME/pm_decisions.jsonl`, manifest/inventory entries, and isolated acceptance cases for ignored user instructions, missing delegation, missing skills/subagents, unreviewed reports, unresolved findings, premature completion claims, worker blame shift, and repeated PM failure.
- Adjusted the V1 guard's authorized repair path classification so user-scoped Harness V2 isolated design/policy/test files and PM decision runtime ledger work are treated as current-task control-plane repair surfaces instead of hard-blocked harness false positives.
- Added reward-hacking heuristic false-positive review support: non-absolute keyword hits in read-only inspection, documentation records, authorized control-plane repair, benchmark fixtures, negative reproduction fixtures, and policy pattern descriptions now become `SUSPECT` with append-only `heuristic_review_jobs.jsonl` records instead of immediate `BLOCKED`.
- Added candidate-only `spark_false_positive_reviewer` configuration and `heuristic_review_reports.jsonl`; reviewer classifications can only inform the parent Stop gate and cannot override absolute deny classes or issue completion authority.
- Preserved absolute deny behavior for secrets/auth access, destructive side effects, unauthorized control-plane mutation, hook disable/weaken/bypass, evaluator/pass-fail manipulation, product hardcoded pass insertion, and warning/error/exit-code hiding.
- Added `Maintenance/Test-HeuristicFalsePositiveReview.ps1` regression coverage and runtime schema/manifest/inventory entries for the new heuristic review ledgers.
- Disabled the global `chrome-devtools` MCP server in `C:\Users\anise\.codex\config.toml` to stop repeated `npx chrome-devtools-mcp` Node.js runtime duplication; Browser Use plugin remains available for local browser work.
- Added hook-routed Spark/high subagent inspection for Harness V2: UserPromptSubmit/PostToolUse/Stop can queue read-only inspector jobs, PostToolUse records candidate-only `subagent_spawn` and `subagent_report` ledger events, and Stop blocks completion-only missing inspector evidence with `required_subagent_not_spawned` or `subagent_report_missing` while PreToolUse stays action-safety only.
- Registered six route-limited Spark inspector roles in `%USERPROFILE%\.codex\config.toml` with read-only role configs under `%USERPROFILE%\.codex\agents`; each inspector is tool-like, route/job-envelope limited, and cannot become completion authority.
- Added `subagent_inspection_jobs.jsonl`, `subagent_inspection_loop_state.json`, Harness V2 acceptance coverage, and `Maintenance/Test-SubagentInspectionRouting.ps1` for direct queue/ledger evidence.
- Removed CONTRACT.SCOPE.PATH_OUTSIDE_ACTIVE_SCOPE as a PreToolUse blocker; path-scope mismatch now stays observational while concrete risk blockers remain active.
- Added isolated Harness V2 prototype materials under `Maintenance/harness-v2`, including design, policy, acceptance tests, prototype runner, migration plan, and GPT Pro discussion note.
- Recorded the review finding that user-mentioned external reference files must be read-only allowed unless private material is detected, while path-scope mismatch is observed instead of blocked; secret/auth, destructive, and unauthorized control-plane actions remain blocked.
- Expanded isolated Harness V2 acceptance coverage for `ASK`, append-only PostToolUse observation, Stop-only required-tool route enforcement, and future-dated receipt rejection.
- Added `Maintenance/harness-v2/harness_v2_integrity_compatibility_actions.md` and root `INVENTORY.md`, then expanded `MANIFEST.json` so isolated Harness V2 artifacts are tracked individually.
- Added Harness V2 policy fields for receipt clock skew and dependency alignment so the isolated policy matches live Stop completion-gate expectations.
- Kept Harness V2 unwired from production hooks pending acceptance and shadow-mode verification.

## 2026-05-06

- Separated active user work scope from read-only Codex runtime reference scope for triggered skill/plugin files and generated image artifacts.
- Added read-only runtime reference paths for `.codex\skills`, `.agents\skills`, `.codex\plugins\cache`, and `.codex\generated_images`; mutation and private material handling remain outside this exception.
- Hardened completion authority by separating agent-written `completion_receipt.json` as candidate input from Stop gate-issued `gate_issued_completion_receipt.json`, with source receipt fingerprint checking.
- Added `future_dated_validation_timestamp` rejection so receipt freshness cannot be self-dated ahead of real validation.
- Made receipt freshness clock tolerance explicit as `allowed_clock_skew_seconds: 5` in the hook, completion policy, runtime schema, and required-tool policy.
- Corrected completion receipt freshness parsing so PowerShell `ConvertFrom-Json` `DateTime` values preserve UTC instead of being stringified and reinterpreted through local time.
- Standardized the tool usage ledger as append-only `tool_usage_event.v2` records with event IDs, nonces, observed timestamps, hook event names, parent lineage, outcome, and tool capability shape; hook invocation logs now also carry event IDs and blocked/failed outcomes.
- Added `Maintenance/Test-EventLedgerIntegrity.ps1` to verify append-only ledger behavior, PreToolUse/PostToolUse/Stop event ID uniqueness, parent lineage preservation, and blocked event recording.
- Fixed `Maintenance/Test-RepoGateAdoption.ps1` strict-mode handling for single hook commands and limited the command guard's Maintenance validation-script repair allowance to the two in-scope verification scripts.
- Added repo gate adoption verification through `repo-gate-adoption.agent.config.yaml`, `Maintenance/Test-RepoGateAdoption.ps1`, and `repo_gate_adoption_receipt.json`, requiring actual hook wiring rather than pattern classification.
- Added Stop-only required-tool routing and receipt validation: `required-tool-routes.json` maps task/path/surface triggers to required tools, skills, MCP discovery, subagents, and checks; `runtime_capability_receipt.json`, `tool_usage_events.jsonl`, and `required_tool_loop_state.json` record capability state, usage observations, and repeated `required_tool_not_used` loop breaks.
- Updated `codex-ssot-hook.ps1` so missing required-tool evidence blocks completion claims with `required_tool_not_used` while leaving PreToolUse ordinary conversation, planning, read-only inspection, and normal implementation work unchanged.
- Added dependency-alignment completion policy: when artifact A changes, connected prompt, resolver, guard, schema, runtime, doc, and reporting surfaces must be checked against the latest A before development completion can be claimed.
- Clarified workflow-document resolution so global/system-contract paths do not require a separate workflow document file. Repo-declared workflows remain preferred only when present in the current repo or explicitly named by the user.
- Updated `codex-ssot-hook.ps1` to recognize additional user-prompt payload keys and to stop treating any `/workflow` path fragment as a finalization artifact signal. This prevents stale or unrelated workflow instructions from making agents report that an optional global workflow document path could not be read.
- Fixed a live `UserPromptSubmit` hook crash where an empty or missing prompt payload could bind as an invalid mandatory string before the hook's `prompt_text_missing` guard ran.
- Added and verified `Maintenance/vibe-coding-contamination-bench.ps1` as an actual-project-derived regression bench for frontend/backend vibe-coding contamination: normal frontend owner-state edits are allowed, while fake product data, rejected legacy sidepanel resurrection, backend test-only static success, repo-local authority weakening, tracked gate-receipt concealment, bare test-pass completion, and subagent PASS completion claims are blocked.
- Changed the bench dry-run invocation to call the hook script directly with `-PayloadJson`, avoiding nested Windows PowerShell JSON argument corruption.
- Added Stop finalization evidence text checking so a fresh receipt alone cannot validate a bare `tests pass` or `subagent PASS` completion claim without paths, spec relation, checks/evidence, and warnings/limits.
- Added repair-scope gating so user-authorized hook/policy/control-plane repairs must stay inside an explicit control-plane path allowlist before they can outrank heuristic contamination keyword checks.
- Added documentation surface classification so non-runtime records such as `CHANGELOG.md` are treated as keyword observations, while runbooks, policy docs, hook configs, and agent instructions remain procedural artifacts.
- Added completion receipt freshness checks requiring the current turn fingerprint, affected paths, artifact hashes, and validation timestamps after the latest relevant write.
- Added dynamic reproduction receipt schema coverage so completion receipts must carry `dynamic_reproduction_check` status, mode, evidence, and related paths for dynamic input to processing to output reproduction.
- Added dynamic reproduction completion-gate and pre-response checklist entries requiring reproduction confirmation, evidence, and paths before `verified_complete` may be claimed.
- Added blocker reason mappings for `dynamic_reproduction_check_missing`, `dynamic_reproduction_failed`, and `dynamic_reproduction_evidence_missing`.
- Added reward-signal finalization nullification so case-tuned success, PASS/test/final/hook pass, and similar finalization signals are contamination candidates rather than completion evidence.
- Recorded the Desktop markdown plan as an execution input only, not a runtime dependency read by the hook runner.
- Restored live `Stop` hook routing after a subagent pause disabled it, and removed the temporary `stop_checks` bypass so completion remains gated by the real receipt checks.
- Narrowed hook-generated prompt detection so automatic `<hook_prompt>` blocker payloads do not invalidate the active user goal while ordinary user analysis text still becomes the current turn oracle.
- Added explicit reward-hacking path blocking for hardcoded success, fake tests, harness manipulation, completion definition tampering, and validation bypass so pass/fail results from those paths are invalid and work must restart through the normal implementation path.
- Added Stop snapshot fallback for subagent or plan-mode-like paths whose writes may not invoke `PreToolUse`; contaminated dummy artifacts, harnesses, validation files, or completion definitions now block completion with `reward_hacking_snapshot_detected`.
- Narrowed live `Stop` gating to block candidate state only when the last assistant message claims completion, allowing non-completion status or blocker explanations to avoid self-reinforcing hook prompt loops while preserving explicit completion gate checks.

## 2026-05-05

- Hardened UserPromptSubmit handling so automatic `<hook_prompt>` blocker messages no longer replace the active user goal, and Codex title-generation wrapper prompts are normalized back to the real user prompt before runtime fingerprints are written.
- Aligned active app config completion states with the SSOT hook state model by replacing `remain` as a state with `blockers`, and corrected the config comment to the actual clean-slate SSOT config path.
- Added frontend evidence policy requiring render or screenshot-based visual review when practical, so tests, verify runs, or completion receipts cannot hide visibly wrong UI.
- Hardened `Settings/Dev_Codex_HOOKS/codex-ssot-hook.ps1` completion checks so `verified_complete` is accepted only when the active contract and completion receipt are both verified, receipt blockers are empty, oracle/scope/evidence fields are confirmed, and protected-surface touch is not unresolved.
- Changed `path_outside_active_scope` from an allowed read-only outcome to a blocker in the pre-command scope guard.
- Made `DO_NOT_CLAIM_COMPLETE` Stop/pre-response outcomes return a Codex-readable block report even when no explicit `CompletionState` argument is provided.
- Added `-NoLog` inspection mode to the hook runner so dry-run analysis can avoid adding hook invocation log entries.
- Added blocker schema mappings and negative reliability cases for implementation-string contract-test slop, stale `verified_complete` claims, and out-of-scope path access.
- Added UserPromptSubmit runtime invalidation: each new prompt now moves the active contract to `in_progress`, turns the previous completion receipt into `candidate`, records a turn fingerprint, and requires final completion receipts to match that fingerprint.
- Filtered UserPromptSubmit active instruction carryover so task-specific flags from a previous turn, such as prior subagent requirements, do not silently become authority for the next user prompt.
- Added `reward-signal-filter.agent.config.yaml` and hook support for filtering frequent work commands (`test`, `verify`, `typecheck`, `lint`, `build`, `rg` absence scans, and hook pass checks) as tic-like uncontrolled slop risk signals until counterexample review clears them.
- Kept reward-signal inspection probes with `-NoLog` state-neutral so hook validation commands do not contaminate the completion receipt they are inspecting.
- Added `control-plane-change-policy.agent.config.yaml` and hook support for separating ordinary product work from authority, hook, policy, manifest, and runtime schema mutations; in-scope control-plane mutations now force candidate state until post-change review clears them.
- Narrowed reward-signal `rg` detection so plain read-only search is observation only, while absence-confirmation scans remain filtered reward-like signals.
- Set the hook runner's console input/output and JSON writes to UTF-8 without BOM to preserve Korean active-contract goals.
- Made `-NoLog` pre-command guard probes state-neutral for both reward-signal and control-plane review registration.
- Updated UserPromptSubmit runtime scoping so the current hook payload `cwd`/`workdir` is added to the active scope, preventing repo-local work from being misclassified as `path_outside_active_scope`.

## 2026-05-04

- Installed `dialkit` and `motion` in `Maintenance/upstream/openai-codex` with `npm install dialkit motion` for frontend tuning work.
- Added frontend tuning defaults to `AGENTS.md`: use DialKit with Motion for live UI, animation, spacing, color, interaction, spring, and easing tuning; mount React `DialRoot` once at the root layout as a sibling to `{children}`; keep production exposure opt-in.

## 2026-05-03

- Added the global Codex MCP server `chrome-devtools` using `npx -y chrome-devtools-mcp@latest` with the local Chrome executable path and usage statistics disabled.
- Loosened the Vowline response-directive/Git boundary in `AGENTS.md`: ordinary Git commit and push are allowed when explicitly requested by the user or required by a repository workflow, while force-push, history rewrite, destructive Git actions, secrets, and protected files remain gated.
- Added shared blocker schema `blocker_schema.v1` to `Settings/Codex_App_DECLARATIVE/clean-slate.agent.config.toml`, including stable `blocker_id`, `reason_code`, and user-facing blocker mappings for completion, command, scope, and credential/security contract failures.
- Updated `Settings/Dev_Codex_HOOKS/codex-ssot-hook.ps1` so blocked reports, Codex-readable hook output, dry-run SSOT output, and future `Maintenance/hook_invocations.jsonl` entries carry the same `schema_version`, `blocker_id`, and `reason_code`.
- Updated hook/reporting/runtime schema declarations so blocked output must name `blocker_id`, `reason_code`, the fired blocker, why it fired, and the safe next action.

## 2026-05-02

- Initialized `Dev_Codex_App_GlobalSSOT` managed element registry with `MANIFEST.json`.
- Added clean-slate declarative Codex App agent configuration at `Settings/Codex_App_DECLARATIVE/clean-slate.agent.config.toml`.
- Established the configuration as an authoritative replacement with legacy imports, fallbacks, bridges, compatibility adapters, and backward compatibility disabled.
- Added workflow orchestration configuration at `Settings/Codex_App_DECLARATIVE/workflow-orchestration.agent.config.yaml` for frame, guard, plan, execute, evidence, review, and report flow.
- Added state machine configuration at `Settings/Codex_App_DECLARATIVE/state-machine.agent.config.yaml` for completion states, transitions, and transition rules.
- Added retry and recovery policy at `Settings/Codex_App_DECLARATIVE/retry-recovery-policy.agent.config.yaml` with fallback disabled and unresolved values required to remain unresolved or blocked.
- Added negative-only agent reliability test configuration at `Settings/Codex_App_DECLARATIVE/agent-reliability-tests.agent.config.yaml` with PASS disallowed as approval, completion, or reward.
- Added cost and latency policy at `Settings/Codex_App_DECLARATIVE/cost-latency-policy.agent.config.yaml` for fast paths, protected paths, subagent defaults, and repeated workflow skill use.
- Added human-in-the-loop policy at `Settings/Codex_App_DECLARATIVE/human-in-the-loop.agent.config.yaml` defining user and agent responsibilities, ask-only conditions, and routine work that should not require user micromanagement.
- Added reporting and SSOT bookkeeping policy at `Settings/Codex_App_DECLARATIVE/reporting-ssot-bookkeeping.agent.config.yaml` for Korean polite reports, completed and blocked response shapes, forbidden reporting claims, manifest updates, changelog updates, root map updates, and runtime data preservation defaults.
- Restored reporting term mapping `blocker` from `remain` to `blocker` in `Settings/Codex_App_DECLARATIVE/reporting-ssot-bookkeeping.agent.config.yaml`.
- Added Dev Codex hook configurations under `Settings/Dev_Codex_HOOKS/` for active contract loading, command guarding, pre-response checks, retry invalidation, completion gating, and post-turn state registration.
- Added Codex App runtime state files and schema declaration under `Settings/Codex_App_RUNTIME/` for active contracts, rejected hypotheses, completion receipts, and stable lessons.
- Added missing root and management documents required for direct read coverage: `README.md`, `Maintenance/README.md`, `Settings/README.md`, `WINDOWS_DEV_TERMS.md`, `콘픽설정.md`, `ROOT_MAP.json`, `NAMING_CONVENTION.md`, `VERSION.md`, `AGENT.md`, `AGENTS.md`, `AGENTS.override.md`, and folder inventories.
- Completed `clean-slate.agent.config.toml` with `[work_budget]` and explicit fallback `route_around = false`.
- Added `Settings/Dev_Codex_HOOKS/codex-ssot-hook.ps1` as the shared executable hook runner.
- Connected `C:\Users\anise\.codex\hooks.json` to the SSOT hook runner for `SessionStart`, `UserPromptSubmit`, `PreToolUse`, and `Stop` hook surfaces while preserving the existing Vowline session-start hook.
- Synced SSOT `AGENTS.md` to the active global `C:\Users\anise\.codex\AGENTS.md` contract after `codex debug prompt-input` confirmed that contract is loaded by Codex.
- Added `Maintenance/hook_invocations.jsonl` as direct hook invocation evidence written by the SSOT hook runner.
- Adjusted the actual `PreToolUse` matcher in `C:\Users\anise\.codex\hooks.json` from a specific tool name to the empty matcher because Codex did not invoke `PreToolUse` for the observed `exec` tool when a specific matcher was used.
- Changed command guard blocking from non-zero hook exit to Codex-readable JSON output `decision = "block"` with a reason.
- Removed unsupported `remote_connections` from `C:\Users\anise\.codex\config.toml` after current Codex feature discovery showed it is not a recognized feature key.
- Reframed `Settings/Codex_App_RUNTIME/active_contract.json` for the current warning, routing, folder contamination, and Windows environment integrity task.
- Added command guard policy coverage for current-task-scoped read-only inspection so subagent read-only audits are not blocked as undeclared generated commands.
- Updated `Settings/Dev_Codex_HOOKS/codex-ssot-hook.ps1` to parse actual Codex hook payloads, allow current-task-scoped read-only inspection, and return empty JSON for allowed `PreToolUse` hooks.
- Disabled stale `C:\Users\anise\AppData\Local\OpenAI\Codex\bin` Codex 0.125 executable entries that shadowed the current Codex 0.128 App package binaries.
- Normalized local plugin metadata warnings by reducing `plugin-eval` default prompts to the supported maximum and shortening the `build-ios-apps` default prompt under the 128-character limit.
- Removed invalid parent-folder icon references from remote plugin sync cache agent metadata that Codex rejects during skill loading.
- Removed the empty obsolete `C:\Users\anise\Documents\Codex\2026-05-02\powershell-dev-codex-app-globalssot-maintainence` folder after verifying it was inside the authorized Codex work folder and contained no children.
- Updated runtime completion state to `blockers` because local remediation is applied but the featured plugin 403 and PowerShell shell snapshot limitation remain outside local SSOT control.
- Consolidated live Codex hooks to one command per event: SessionStart, UserPromptSubmit, PreToolUse, and Stop now each run one hook command instead of 2/2/1/3.
- Moved Vowline startup context into the SSOT SessionStart hook output and removed the duplicate `.codex\skills\vowline` copy after matching it to the canonical `.agents` skill by SHA256.
- Added `user_prompt_submit` and `stop_checks` consolidated hook names and a runtime fast path to `Settings/Dev_Codex_HOOKS/codex-ssot-hook.ps1` so live hooks avoid repeated full SSOT scans.
- Reduced plugin config and cache to `spreadsheets@openai-primary-runtime`, `github@openai-curated`, and `browser-use@openai-bundled`; removed unrequested plugin cache folders and pruned `.tmp\plugins` to the local GitHub marketplace entry.
- Removed empty `.codex\memories`, `.codex\shell_snapshots`, `.codex\worktrees`, and empty Git internal folders under the authorized Codex environment.
- Recorded remaining blockers: featured plugin 403 persists when `features.plugins = true`, while `codex exec --disable plugins` suppresses it; PowerShell shell snapshots remain unsupported by Codex 0.128.

## 2026-05-02 - Canonical root and plugin cache correction

- Moved active Dev_Codex_App_GlobalSSOT canonical root to %USERPROFILE%\.codex\Dev_Codex_App_GlobalSSOT and removed the dated Documents canonical-root copy.
- Retargeted %USERPROFILE%\.codex\hooks.json to the user Codex SSOT hook runner and verified dry-run root resolution there.
- Repaired pre-command guard handling for active-scope work so user-scoped global maintenance is not misreported as `path_outside_active_scope` when it stays inside the active contract.
- Created stable local plugin marketplaces under %USERPROFILE%\.codex\plugins\local-marketplaces containing only `browser-use`, `github`, and `spreadsheets`; removed `latex-tectonic`, `documents`, `presentations`, stale runtime install payload, `.tmp` remote sync clones, and empty memory placeholder.
- Recorded blocker: Codex 0.128.0-alpha.1 still performs built-in remote featured plugin warm against `chatgpt.com/backend-api/plugins/featured` whenever `features.plugins` = true; this returns Cloudflare 403 and can recreate `.tmp\plugins` with unrequested remote marketplace entries during execution.
- Enabled the `/goal` slash command in the active Codex config and mirrored the intent in `Settings/Codex_App_DECLARATIVE/clean-slate.agent.config.toml`.
- Installed the `keep-codex-fast` skill from `https://github.com/vibeforge1111/keep-codex-fast`, ran its report-only inspection, and added Codex handoff/reactivation documents under `docs/codex-handoffs/` before any session archiving.
- Cloned the official `openai/codex` repository into `Maintenance/upstream/openai-codex`, inspected its AGENTS, install, config, justfile, package, and argument-comment-lint workflows, rewrote `AGENT.md` as role/purpose/behavior principles, and added `Settings/Codex_App_DECLARATIVE/tool-skill-subagent-mcp-usage.agent.config.yaml` so tools, skills, subagents, MCP, and repo-declared workflows are actual-use requirements rather than passive permissions.
- Enabled the recognized `child_agents_md` feature in `C:\Users\anise\.codex\config.toml` and expanded the usage policy with official Codex findings for `tool_search`-first MCP discovery, MCP resource boundaries, deferred MCP exposure, sanitized MCP tool naming, skill triggering/config facts, and subagent lifecycle handling.
- Added explicit failed-state reporting to `Settings/Dev_Codex_HOOKS/codex-ssot-hook.ps1`: blocked hook outcomes now include the fired hook, fail policy, fired blocker, raw reason, why it fired, safe next action, current blockers, and evidence instead of only a generic reason code.
- Updated completion/pre-response/reporting schema config so blocker outcomes must name the fired blocker and why; `CompletionState=blockers` now returns a Codex-readable block report for `completion_gate` while `verified_complete` remains allowed.
- Installed `git-easy-korean` under `%USERPROFILE%\.codex\skills` from `C:\Users\anise\Desktop\개발환경\바이브깃.md`, and updated SSOT/global agent instructions so Git/GitHub work loads that skill and reports user-facing Git state in easy Korean terms.
- Installed the previously missing Codex Rust workflow tools on the Windows native path: `rustup`, `cargo`, `rustc`, `just`, `bazel`, `dotslash`, `cargo-nextest`, `cargo-insta`, `cargo-dylint`, and `dylint-link`; updated the tool usage policy to mark the workflow as Windows native only.
- Removed the accidental `Ubuntu-24.04` WSL distribution created during toolchain setup after the user clarified that work should stay Windows native only.
- Completed the Windows native JS/TS, Python, helper CLI, and native build tool setup: installed or verified `node`, `npm`, `corepack`, `pnpm`, `yarn`, `tsc`, `tsx`, `biome`, `prettier`, `eslint`, `pyright`, `bun`, `deno`, `python`, `pip`, `uv`, `pipx`, `ruff`, `mypy`, `pytest`, `black`, `poetry`, `pdm`, `tox`, `nox`, `jq`, `yq`, `rg`, `fd`, `bat`, `fzf`, `delta`, `hyperfine`, `sccache`, `cmake`, `ninja`, Visual Studio Build Tools `cl`/`link`, PowerShell, VS Code, and PowerToys.
- Added `C:\Users\anise\.codex\profile.d\WindowsNativeDev.ps1` and imported it from `C:\Users\anise\Documents\PowerShell\profile.ps1` so new PowerShell sessions automatically include common native helper paths and can run `cdev` / `Enter-WindowsNativeDev` to load the MSVC native build environment.

## 2026-05-02 - Warp auxiliary PowerShell adoption

- Installed Warp as an auxiliary PowerShell surface with `winget install --id Warp.Warp --exact --accept-package-agreements --accept-source-agreements`; verified installed package `Warp.Warp` version `v0.2026.04.27.15.32.stable_03`.
- Recorded the executable location as `%LOCALAPPDATA%\Programs\Warp\warp.exe` and confirmed Warp user data folders under `%LOCALAPPDATA%\warp` and `%APPDATA%\warp`.
- Added `Settings/Codex_App_DECLARATIVE/warp-assistant-terminal.agent.config.yaml` to limit Warp use to Codex and Codex-authorized subagents for active user-scoped work, keep Warp auxiliary-only, block Warp Oz/cloud/autonomous agents, and require first-run UI settings review to disable Warp AI, Active AI, natural-language detection, telemetry, crash reporting, and codebase autoindexing.
- Added Warp auxiliary scaffolding under `Tools/Warp/` and exposed `warpaux` through `%USERPROFILE%\.codex\profile.d\WarpAuxiliary.ps1` from the PowerShell profile.
- Changed Warp `settings.toml` to disable Warp AI, cloud conversation storage, settings sync, third-party agent toolbar, code assistant UI, Warp Drive, telemetry, and crash reporting, and set the default session mode to terminal.
- Set the auxiliary launch policy so Warp opens minimized and the previously active foreground window is restored unless foreground use is explicitly requested.

## 2026-05-02 - Codex home light cleanup

- Moved reversible cleanup candidates out of `%USERPROFILE%\.codex` while Codex was still running: `maintenance-archives`, `.tmp\bundled-marketplaces\openai-bundled\plugins\latex-tectonic`, and `.tmp\plugins`.
- Preserved the moved items under `C:\Users\anise\Documents\Codex\codex-backups\manual-codex-cleanup-20260502-211550` with `moved-items.jsonl` and `restore-moved-items.ps1`.
- Left live session, state, auth, config, skills, plugins, and log SQLite files untouched because Codex was actively running.
