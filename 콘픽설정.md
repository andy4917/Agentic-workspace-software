# 콘픽설정

`Settings/Codex_App_DECLARATIVE/clean-slate.agent.config.toml` is the clean-slate config layer.

The config layer contains machine-readable policy values only. Explanatory text belongs in README, maintenance, changelog, or naming documents.

